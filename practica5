use farmacia
select * from producto


SELECT Nombre
FROM Producto 
WHERE Precio > 80 
GROUP BY Nombre

SELECT Precio, MAX(Precio) as PrecioMaximo, MIN(Precio) as PrecioMinimo, SUM(Precio) as SumaPrecios
FROM Producto
GROUP BY Precio
